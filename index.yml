---

version: 1

tools:
  kubectl:
    metadata: tools/container/kubectl.yml

  terraform:
    metadata: tools/iac/terraform.yml

  k9s:
    metadata: tools/container/k9s.yml

  task:
    metadata: tools/ci/task.yml

  helm:
    metadata: tools/container/helm.yml

  kubent:
    metadata: tools/container/kubent.yml